info:
  name: create
  type: http
  seq: 2

http:
  method: POST
  url: "{{users}}"
  body:
    type: json
    data: |-
      {
        "email": "email@example.com",
        "password": "abc123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
