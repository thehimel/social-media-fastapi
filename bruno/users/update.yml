info:
  name: update
  type: http
  seq: 5

http:
  method: PUT
  url: "{{users}}/1"
  body:
    type: json
    data: |-
      {
        "title": "top beaches in florida",
        "content": "check out these awesome beaches",
        "published": true,
        "rating": 5
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
