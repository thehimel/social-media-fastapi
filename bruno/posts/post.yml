info:
  name: post
  type: http
  seq: 3

http:
  method: GET
  url: "{{posts}}/2"
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
