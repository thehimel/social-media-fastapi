info:
  name: delete
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{posts}}/1"
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
