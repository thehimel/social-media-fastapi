info:
  name: vote-delete
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{posts}}/2/vote"
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
