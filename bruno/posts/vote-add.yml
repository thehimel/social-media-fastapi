info:
  name: vote-add
  type: http
  seq: 7

http:
  method: POST
  url: "{{posts}}/2/vote"
  body:
    type: json
    data: |-
      {
        "title": "top beaches in florida",
        "content": "check out these awesome beaches"
      }
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
