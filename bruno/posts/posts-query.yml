info:
  name: posts-query
  type: http
  seq: 6

http:
  method: GET
  url: "{{posts}}/?limit=2&skip=1&search=beaches"
  params:
    - name: limit
      value: "2"
      type: query
    - name: skip
      value: "1"
      type: query
    - name: search
      value: beaches
      type: query
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
