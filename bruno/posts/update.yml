info:
  name: update
  type: http
  seq: 5

http:
  method: PUT
  url: "{{posts}}/1"
  body:
    type: json
    data: |-
      {
        "title": "top beaches in florida",
        "content": "check out these awesome beaches",
        "published": true,
        "rating": 5
      }
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
