name: local
variables:
  - name: host
    value: http://127.0.0.1:8000
  - name: posts
    value: "{{host}}/api/posts"
  - name: users
    value: "{{host}}/api/users"
  - name: auth
    value: "{{host}}/api/auth"
  - secret: true
    name: access_token
