info:
  name: login
  type: http
  seq: 6

http:
  method: POST
  url: "{{auth}}/login"
  body:
    type: form-urlencoded
    data:
      - name: username
        value: email@example.com
      - name: password
        value: abc123
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
