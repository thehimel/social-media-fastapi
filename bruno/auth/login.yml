info:
  name: login
  type: http
  seq: 6

http:
  method: POST
  url: "{{auth}}/login"
  body:
    type: json
    data: |-
      {
        "email": "email@example.com",
        "password": "abc123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
